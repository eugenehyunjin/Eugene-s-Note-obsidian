해야할 일:
- [x] 6399 명지 이노매트릭스 연동 관련 오류 체크 - 전월 입차, 이번 월 출차 시, vehicle_detail 페이지에서 보여지는 부분에 대해 처리 해 놓음, 관련된 내용 상무님에게 전달 / 17일 저녁 7시 이후를 기준으로 이노매트릭스에서 입차 처리에 대한 로직 수정 / 3월 전체 방문중 오류 차량 내역 전달 완료(약 600여건), 비원이미지의 정보도 전달 완료(약 90여건), 비원이미지 서버 오류 관련 체크 완료 후 복구 완료, 이노메트릭스  
- [x] 차량 관제 시스템 자동화 개발/ 혜림님 1차 소스 코드 깃에 적용 확인
- [ ] 주민소통방 업로드된 글 사진 용량이 클 때 화면 인쇄 오류 부분 처리해주기/ 다음주로 딜레이 
- [x] 아파트리 DB 백업해주기 3월 29일 파일 백업
- [x] 아파트리 홈페이지 디자인 리뉴얼 최종 배포하기 04/04 최종 배포
- [x] 아파트리 홈페이지 핸드폰 목업 디자인 교체하기
- [x] 아파트리 앱 기획서 피그마 추가 작업(화,목)

이번주 반드시 처리 할 일:
- [x] 과장님 전달 주신 이력서 검토 후에 피드백 전달하기, 추가 이력서 7건 확인

```
grant all privileges on *.* TO 'aptree'@'203.235.19.242' identified by 'aortlazksn@#7898@#';
```


SELECT * FROM vehicle_histories WHERE apartment_idx = "6084" AND type = "visit" ORDER BY in_date desc;


EXPLAIN 
SELECT A.APARTMENT_IDX, 
       '20250402' TRANS_DATE, 
       '19029' TRANS_NO, 
       A.IDX, 
       'N' APPLY_YN
FROM VISITING_VEHICLES A
WHERE A.APARTMENT_IDX = '6084'
AND NOT EXISTS (
    SELECT 1 
    FROM VISITING_VEHICLES_TRANS X 
    WHERE X.APARTMENT_IDX = A.APARTMENT_IDX 
    AND X.VISITING_VEHICLES_IDX = A.IDX 
    AND X.APPLY_YN = 'Y'
);


http://localhost:8280/visit/trans/6084


EXPLAIN 
SELECT A.APARTMENT_IDX
FROM VISITING_VEHICLES A
LEFT JOIN VISITING_VEHICLES_TRANS X 
ON A.APARTMENT_IDX = X.APARTMENT_IDX
AND A.IDX = X.VISITING_VEHICLES_IDX
AND X.APPLY_YN = 'Y'
WHERE A.APARTMENT_IDX = '6084'
AND X.APARTMENT_IDX IS NULL


6399단지 출차 정보 없는 차량 내역 검색 쿼리
SELECT vh.*
FROM vehicle_histories vh
JOIN visiting_vehicles vv 
    ON vh.car_number = vv.car_number
WHERE vh.apartment_idx = '5989'
AND vh.out_date IS NULL
AND vh.in_date < '2025-04-01'
AND vh.in_date BETWEEN vv.scheduled_date AND vv.out_date
AND vh.use_flag = "Y"
;



SELECT vh.*
FROM vehicle_histories vh
JOIN visiting_vehicles vv 
    ON vh.car_number = vv.car_number
WHERE vh.apartment_idx = '6084'
AND vh.out_date IS NULL
AND vh.in_date < '2025-04-01'
AND vh.in_date BETWEEN vv.scheduled_date AND vv.out_date
AND vh.use_flag = "Y"
ORDER BY in_date asc
;


윈도우 서버 244 frp 사이트 생성 과정
1. https://dungdung-developer.tistory.com/7
2. 위의 링크 참고하여 생성

이슈:
1. 전체 세팅을 완료해도 적용이 되지 않음, port가 열려있지 않은 것이 문제, 방화벽에서 세팅을 해줘도 powershell에서 직접 명령어로 열어줘야 함
```
21포트 열어주기
netsh advfirewall firewall add rule name="FTP Server (Port 21)" protocol=TCP dir=in localport=21 action=allow

사용가능한 포트 열어주기(ftp 패시브 모드)
netsh advfirewall firewall add rule name="FTP Passive Mode" protocol=TCP dir=in localport=50000-51000 action=allow

ftp 패시브 모드 세팅 내용 확인
netsh advfirewall firewall show rule name="FTP Passive Mode"

ftp 규칙 내용 확인
Get-NetFirewallRule | Where-Object { $_.DisplayName -like "*FTP*" }

모든 인바운드, 아웃바운드 열어주기
Set-NetFirewallRule -Name "IIS-WebServerRole-FTP-In-TCP-21" -Enabled True
Set-NetFirewallRule -Name "IIS-WebServerRole-FTP-Passive-In-TCP" -Enabled True
Set-NetFirewallRule -Name "IIS-WebServerRole-FTP-Out-TCP-20" -Enabled True
Set-NetFirewallRule -Name "{E8585E13-B290-471C-A57D-A4DAA0E55F23}" -Enabled True
Set-NetFirewallRule -Name "IIS-WebServerRole-FTP-Out-TCP-989" -Enabled True
Set-NetFirewallRule -Name "IIS-WebServerRole-FTP-In-TCP-990" -Enabled True

최종 세팅된 정책 업데이트
gpupdate /force

여기서 주의할 점은 
netsh advfirewall reset 명령어로 방화벽 리셋을 해버리면, 원격 접속 관련된 방화벽도 리셋이 되어버려서, 원격이 끊어짐(여기서 식은땀 한방울), 걱정하지 말고 동일한 네트워크에 연결된 서버에 다시 원격 접속(여기서는 안전하네 243 서버에 접속), 마찬가지로 powershell을 열고 아래의 명령어 실행하여 244 서버에 접속

해당 서버가 winRM활성화 되어있는지 확인
Test-WSMan -ComputerName 203.235.19.244

가능할 경우 아래의 응답 확인 가능
wsmid: http://schemas.dmtf.org/wbem/wsman/identity/1
불가능: Test-WSMan : 연결할 수 없습니다.

가능했기 때문에 아래에 명령어로 접속 시도
Enter-PSSession -ComputerName 203.235.19.244 -Credential 관리자계정
Enter-PSSession -ComputerName 203.235.19.244 -Credential Administrator

관리자 계정은 Administrator로 비번 입력 창이 활성화 됨 그러면 원격 접속 비번 입력
오류가 발생할 수 있음 원인은 해당 ip의 신뢰도가 없어서 그러면 아래의 명령어로 244 서버 접속
신뢰 체크 해줌

Set-Item wsman:\localhost\Client\TrustedHosts -Value "203.235.19.244" -Force

이제 다시 아래 명령어 실행
Enter-PSSession -ComputerName 203.235.19.244 -Credential Administrator

244에 접속이 되면 파워쉘에서 프롬프트가 아래 처럼 변경된
[203.235.19.244]: PS C:\Users\Administrator>

이제 접속이 되었으니, 아래의 명령어로 다시 원격접속 포트를 열어줌
netsh advfirewall firewall add rule name="Remote Desktop" protocol=TCP dir=in localport=3389 action=allow

이제 다시 원격 접속 가능하게 되고, 원격으로 접속하여 위에 순서대로 다시 ftp 접속 허용 처리를 진행해 줍니다.

```



**리뉴얼 웹**
구글 앱 다운로드(메인 비주얼) - google_mv_btn_click 애플 앱 다운로드(메인 비주얼) - apple_mv_btn_click 
구글 앱 다운로드(CTA) - google_cta_btn_click 애플 앱 다운로드(CTA) - apple_cta_btn_click

복지 몰(메인 비주얼) - mall_mv_btn_click 복지 몰(모바일 메뉴) - mall_menu_btn_click


기능 적 부분
배포완료
home/member/forgot_user_id_done.php
home/member/forgot_user_done.php
home/member/forgot_user_search.php - 검색된 아파트 단지 텍스트 변경
js/mins-2.js / 로그인 관련
index2.php 최종 본
member/logout.php 로그아웃 후 링크 / 으로 변경


이전에 전달 주신 10통 이력서 검토 결과 아래 개발자님들 추천드립니다.:)
1. 김동찬님(창업 관련 수상 경력)
2. 김준형님(마케팅 경력, 디자인 가능)
3. 박미정님(경력), 
4. 황진하님
이지은님은 경력이셔서  이력서 내용 외에 포트폴리오가 첨부가 되어있지 않아서, 혹시 포트폴리오 확인이 가능한지 문의 드립니다.:)

오늘 주신 7건의 이력서는 다음주에 검토진행해 보도록 하겠습니다.

