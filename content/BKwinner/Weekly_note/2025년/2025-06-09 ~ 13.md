- [x] 앱 개발 관련 wbs 작성
- [x] 관리자 페이지 마케팅 최종 배포
- [x] 관리자 페이지 제휴 영역에 대한 GA, GTM 세팅 , 세팅 완료 후 연지님에게 공유 완료
- [x] 전자투표완료 배너 이미지 전청당 종료 후 수정
- [x] 239 서버 아마노  단지 자릿수 3자리로 가능하도록 수정 = 아마노 방효수 담당자님과 메일로 3자리도 무관하다는 답변 받아서 센터에 공유 완료
- [x] 웹/앱의 개인정보처리방침 내용 수정
- [x] 부산웹 장대표님 쇼핑몰 관련 통화 
- [ ] 아파트리 쇼핑몰 웹/앱 링크 연결 및 SSO 기능 검토(현재 입주민 앱에서 검토 필요)
- [x] 13일 금요일 전체 회의(각자 맡아서 검토 및 진행해온 내용 공유 및 앞으로의 작업 스택 결정)


전청당 뮤지컬 배너 클릭 수(5/20 ~ 현재)
전자 투표 완료 페이지 
344건 + 120건 + 8건,  합계 472건
앱/웹 배너
151건

합계: 623건

ga id aptree-web:
G-VDPSZ4Z9Z4

ga id aptree-admin:
G-1FDHPT229L

보험금청구를 위해 병원 서류를 떼고 창구를 방문하는 시대는 이제 달라지고 있습니다. 신한 라이프는 고객의 생체 데이터를 기반으로 보험이 먼저 반응하고 스스로 작동하는 새로운 시스템을 선보였습니다. 이제는 말하지 않아도 보험이 알아서 움직이는 시대가 시작되고 있습니다. 스크립트만 준비해봐 이제 당신도 ai 유투버


window.open('https://vpms2.dozn.co.kr/HP_Statement.aspx?pParam=Statement&pDanCode=<?=$_AC["id"]?>&pYyyymm='+DAT, '_blank', ''); // 20220421, 2023-12-08


플러터 실행 명령어
https://github.com/eugenehyunjin/flutter-test-app.git
명령어:
1. flutter pub get
2. flutter build apk --debug
에뮬레이터 실행 전에 크롬에서 실행해도 괜찮습니다.:)



입주민 앱 로컬 세팅 완벽 정리 순서:
1. 인텔리제이 git clone
https://gitlab.com/b5515/apt_user_app.git
branch: hl-branch
깃에 반영 해야 할 사항: 
- 혜림님 로컬의 package.json 파일 깃에 반영 필요(현재 라이브러리 추가 안되어 실행 오류 발생)
- android/app/build.gradle 파일 깃에 반영 필요(버전 관리)

2. 안드로이드 스튜디오에서 프로젝트 실행
- 필요한 인프라 모두 설치(avd 설치, sdk 설치, node, java, nvm 설치)
```
버전 정보:
java: 11
node: 16
```

3. 아래의 명령어 순서대로 실행 
rm -rf node_modules (기존 라이브러리가 설치 되어있다면 삭제)
npm install --legacy-peer-deps (최신 packge.json 라이브러리 설치)
cd android && ./gradlew clean (gradle 클린 및 빌드)
cd .. && npx react-native run-android (애뮬 실행)

4. 발생 가능한 문제
-  C 드라이브 용량이 너무 꽉차 있는 경우 아래의 폴더에서 캐시 삭제
- 한 번 실행했었다면 프로그램이 사용중이라 삭제 안되는 경우 아래 명령어로 java 프로세스 종료 후 삭제 진행
- taskkill /F /IM java.exe /T
## 📂 디스크 공간을 차지하는 주요 경로

| 목적                        | 경로                                                          | 삭제 가능 여부                       |
| ------------------------- | ----------------------------------------------------------- | ------------------------------ |
| Android 빌드 결과물            | `android/app/build`                                         | ✅ 삭제 가능                        |
| 전체 Android 빌드 캐시          | `android/build`                                             | ✅ 삭제 가능                        |
| Gradle 캐시                 | `~/.gradle/caches` (Windows: `C:\Users\사용자\.gradle\caches`) | ✅ (삭제 가능하나 다시 다운로드함)           |
| npm/yarn 모듈               | `node_modules`                                              | ✅ (삭제 후 `npm install`로 재설치 가능) |
| Metro 번들 캐시               | `~/.metro-cache`                                            | ✅ 삭제 가능                        |
| Watchman 캐시 (macOS/Linux) | `~/.watchman`                                               | ✅ 삭제 가능                        |
| Emulator 자체의 이미지/스냅샷      | `~/.android/avd/`                                           | ❌ 조심 (AVD 삭제됨)                 |
|                           |                                                             |                                |
- js 번들이 없어서, metro 서버와 애뮬이 연결이 안되는 경우, 번들 생성 명령어를 통해서 번들 생성해주기 (이번에 해결된 방법)
```
오류 로그
# Unable to load script. Make sure you're either running a Metro server (run 'react-native start') or that your bundle 'index.android.bundle' is packaged correctly for release.

번들 생성 명령어:
react-native bundle --platform android --dev false --entry-file index.js --bundle-output android/app/src/main/assets/index.android.bundle
```

- 애뮬과 metro 서버 포트 연결이 안되는 경우 다시 설정
```
adb reverse tcp:8081 tcp:8081
```


## 🧼 자주 사용하는 명령어 정리

| 명령어                                    | 설명                              |
| -------------------------------------- | ------------------------------- |
| `npx react-native start --reset-cache` | Metro 번들러 실행 (자바스크립트 서버)        |
| `npx react-native run-android`         | Android 에뮬레이터 또는 연결된 디바이스에 앱 실행 |
| `npx react-native run-ios`             | iOS 시뮬레이터에 앱 실행 (Mac 필요)        |
| `npx react-native-clean-project`       | 프로젝트를 완전 초기화 수준으로 클린            |
| `npm install` / `yarn install`         | Node.js 의존성 설치                  |
| `./gradlew clean`                      | Android 빌드 캐시 삭제                |
| `npx pod-install`                      | iOS용 라이브러리 설치 (Podfile 기반)      |
| `npm run patch-package`                | 의존성 패키지를 수정한 후 패치 유지            |
| `react-native doctor`                  | 개발 환경 문제 자동 점검                  |
| `npm audit fix`                        | 보안 취약점 자동 수정                    |
| `adb shell input keyevent 82`          | 애뮬 강제로 dev 세팅 실행                |


혜림씨 깃에 반영 필요한 파일:
android\gradle\wrapper\gradle-wrapper.properties
android/app/build.gradle
android/build.gradle
package.json

혜림님:
플러터, 타임리프 방식 비교:
개발속도가 빠름, 자바스크립트 브릿지를 통해서 데이터를 주고 받는 부분에서 앱 로딩 지연 발생
플러터 웹뷰 방식을 추천
core:
네트워크 dio 방식 사용 - 오버라이딩해서 사용하도록 처리
라우터 -  화면 이동할 때 사용
토큰 스토리지 - 앱 삭제 시에만 사라지는 부분 
테마 - 테마 변수를 만들어서 컴포넌트에 조합하는 방식, 하드코딩 안하고 일관성 있는 ui 개발 가능

ui:
프레젠테이션 - 전체 ui에 대한 
프로바이더(컨트롤러, 서비스 역할), 스크린스(뷰 역할)
data:
레포지토리: 백엔드 api 요청, 응답 받아서 데이터를 예외 처리
프로바이더: 스토리지(토큰) 확인해서, 상태 업데이트, 로그인 스크린으로 넘김

동찬님:
244서버를 어떻게 활용?
gitflow - develop에서부터 테스트 코드가 들어가면 안됨
통합테스트 환경을 디벨롭으로 마련하게 되었고, 디벨롭 브랜치는 어떠한 수정이 일어나도 상관 없음, 리팩토링 모두 가능, 메인에서 따게 됨, 릴리즈에 올린 다음에, 통합환경 테스트 하고 메인으로 올리는 과정
릴리즈의 생존 주기 - 매주 목요일 업데이트(종합적으로 검토 후 배포)

넘버링 - 툴의 티켓들에 대한 넘버링을 식별자로 사용하기 - 깃랩의 이슈를 사용해서 업무 내용으로 태스크를 생성할 수 있음, 업무 공유에 대한 보드로 활용가능할 것 같음 '#'으로 시작하는 넘버를 활용

노션에서 가시성 좋게 사용가능한가? - 진행 상태를 만들어 놓은 것을 운용하는게 어떨까?  깃랩 이슈를 노션으로 가져오는 기능 사용

CI/CD: 브랜치 - 서버 연결? 테스트를 할 것이냐? 깃랩 러너 설치? 엔진 판별을 못하고 있음
빌드 자체에 대해서는 244서버에서 테스트 진행해보고, 번들 만들어서, 앱스토어에 올리는 엔진 설치해서 임시로 할 수 있음

QA 릴리즈 수정 하고, 메인으로 넘길 때, 수동으로 진행하기
피처 브랜치는 1~2주 지나고 삭제 하기

추가 문의:
테스트 - 윈도우, 실배포 - 리눅스 고려 해서 테스트 진행해 보셔야 할 것
깃랩 이슈는 - 연지님은 회사 계정으로 사용하도록 할 것
사장님 설명 해 드리기
다음주에 오늘 해주신 프론트, 백엔드 코드는 깃에 업로드하고, 깃플로우 정책에 맞춰서 기본 브랜치 설정하기 작업

동서에서 전달 받은 이미지? 텍스트를 전달 받음