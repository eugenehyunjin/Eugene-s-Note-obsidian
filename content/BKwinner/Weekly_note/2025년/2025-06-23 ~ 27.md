해야 할 일:
- [x] 월요일 회의
- [ ] 금요일 전체 회의 14층 회의실에서 진행
- [ ] 열려라 참깨 개발
- [ ] 다운텍 차량 관제 테스트 진행
- [x] 앱 리뉴얼 웹뷰 개발
- [x] 밤바캐스트 팝업 설정
- [x] 플러터 프론트 작업


특이사항:
- 수요일 연지님 연차
- alarm Aptree 기능 관련 문의 해보기 - 사장님 


20556

/ INSERT INTO vehicle_histories (created_by, 
updated_by, apartment_idx, dong, ho, dongho_idx, car_number, type, in_date, use_flag) select 
'1','1', a.idx as apartment_idx, b.dong, b.ho, b.idx as dongho_idx, '09누4999', 'visit', STR_TO_DATE('20250621162734', 
'%Y%m%d%H%i%s') as IN_DATE, 'Y' from apartments a join donghos b ON (a.idx = b.apartment_idx) 
where a.apt_code = '986' and b.dong = '201' and b.ho = '302

SELECT
us.*,at.apt_name,dh.dong,dh.ho
from
users us
left join donghos dh on(us.dongho_idx=dh.idx)
left join apartments at on(us.apartment_idx=at.idx)
where 1=1
and us.apartment_idx = "7434"
order by created_at desc

$danjiCode = isset($_SESSION['my_dancode']) ? $_SESSION['my_dancode'] : "9500";


Navigator.push(
  context,
  MaterialPageRoute(
    builder: (_) => WebViewScreen(
      config: WebViewConfig(
        url: 'https://aptree.co.kr/home/mall',
        method: 'GET',
        params: {'userId': '1234'},
      ),
    ),
  ),
);
211.54.148.249

깃 문제:
untracked files prevent merge move or commit them before merge  문제 해결
git clean -d -f -f



apt9500
eco004

**1. SK쉴더스 제주삼화아너스베뉴주차장**

A. 서버 URL : [http://a21382.c](http://a14864.codns.com/)odns.com:9948

B. 주차장 번호 : 21382

C. 할인코드 : 88 (전액할인) 

D. 기본할인시간 :  

F. 단지 코드 : 5666

Basic YW1hbm86YW1hbm8=
CAR_REGISTER

xampp 경로 변경 시 오류 사항:
user/9500/lib/config.php 파일에서 아래의 소스 코드에서 경로가 하드코딩 되어있어서 오류 발생, 변경한 경로로 수정 하면 처리 완료
include_once "D:\\xampp\\htdocs\\home\\lib\\_modules-lib\\config.php";
D:\Android\Sdk

C:\Users\BKW_RnD_2\AppData\Local\Android\Sdk

비상연락망:
1. 대표이사	권두영	010-7181-3010	
2. 상무이사	유홍	  010-8519-3451	
3. 관리과장	이지은	010-4808-0558
4. 연구개발 	이현진	010-5000-1193
5. 사원	윤혜림	010-4179-7231
6. 사원	손연지	010-3137-2772
7. 지원센터	문상순	010-8559-8818
8. 사원	윤정희	010-3889-4184
9. 관리과장	서은정	010-7182-4219
10. 사원 박상언  010-5864-5384
11. 사원 김동찬  010-2547-1217

 AmanoInOutCar 요청 파라미터: {eqpmID=12, inDtm=20250625175741, isCustDef=false, iID=3411, companyName=, dongcode=, eventType=ExitedCar, carImagePath=http://118.43.197.122/image/20250625/CH02_20250625191653_28수4541_02_FR.jpg, userName=, parkingName=삼화아너스베뉴, carNumber=28수4541, lotArea=21382, passType=normal, historyID=14551, hocode=, lprTrnsID=14556, custDefUserID=0, eventTime=20250625191653, eventName=Exited car event, custDcUserID=0, inEqpmID=11, aptIdx=5666, isCustDc=false}

플러터 관련:
1. 실제 DB 연결과 관리를 위해서 env 설정을 해놓으면 좋을 것 같음
2. 웹뷰 연결 확인 완료(쇼핑몰, 이제너두)

13:57:15 등록
13:57:42 입차 신호
13:57:44 입차 신호

14:31:38 아웃 신호(동호 포함)

방문예약 인덱스
151501는 삭제된 상태

밤바캐스트 관련 상담 문의 접수 되어 공유드립니다.:) 오늘 중으로 연락 주시면 감사하겠습니다.
010-5207-6335
wwbb2002@naver.com
우방2차신세계타운
방송설치 및 사용 견적서관련 문의드립니다.

import 'package:apt_user_flutter/core/theme/app_colors.dart';  
import 'package:apt_user_flutter/core/theme/app_text_styles.dart';  
import 'package:flutter/material.dart';  
import 'package:flutter_svg/svg.dart';  
  
class AppAppBar extends StatelessWidget implements PreferredSizeWidget {  
  final String? title;  
  final bool showBackButton;  
  final VoidCallback? onBackPressed;  
  
  const AppAppBar({  
    Key? key,  
    this.title,  
    this.showBackButton = false,  
    this.onBackPressed,  
  }) : super(key: key);  
  
  bool get isHome => title == null || title!.isEmpty;  
  final unreadCount = 3;  
  
  @override  
  Widget build(BuildContext context) {  
    return AppBar(  
      backgroundColor: Colors.white,  
      elevation: 0,  
      centerTitle: true,  
      leading: showBackButton  
          ? IconButton(  
        icon: const Icon(Icons.arrow_back_ios_new, color: Colors.black),  
        onPressed: onBackPressed ?? () => Navigator.of(context).pop(),  
      )  
          : null,  
      title: isHome  
          ? SvgPicture.asset(  
        'assets/images/logos/app_bar_logo.svg',  
        height: 22,  
        fit: BoxFit.contain,  
      )  
          : Text(  
        title!,  
        style: AppTextStyles.h3.copyWith(  
          color: AppColors.primaryText,  
          fontWeight: FontWeight.w600,  
        ),  
      ),  
      actions: [  
        Stack(  
          children: [  
            IconButton(  
                onPressed: () {  
                  ScaffoldMessenger.of(  
                    context,  
                  ).showSnackBar(SnackBar(content: Text('알림 페이지로 이동')));  
                },  
                icon: SvgPicture.asset(  
                  'assets/images/icons/basic/bell.svg',  
                  width: 30,  
                  height: 30,  
                )  
            ),  
            if (unreadCount > 0)  
              Positioned(  
                right: 8,  
                top: 8,  
                child: Container(  
                  padding: EdgeInsets.all(2),  
                  decoration: BoxDecoration(  
                    color: AppColors.error,  
                    borderRadius: BorderRadius.circular(10),  
                  ),  
                  constraints: BoxConstraints(minWidth: 16, minHeight: 16),  
                  child: Text(  
                    '$unreadCount',  
                    style: TextStyle(  
                      color: Colors.white,  
                      fontSize: 10,  
                      fontWeight: FontWeight.bold,  
                    ),  
                    textAlign: TextAlign.center,  
                  ),  
                ),  
              ),  
          ],  
        ),  
      ],  
    );  
  }  
  
  @override  
  Size get preferredSize => const Size.fromHeight(kToolbarHeight);  
}


import 'package:apt_user_flutter/features/auth/signin/signin_screen.dart';  
import 'package:go_router/go_router.dart';  
  
import '../../core/constants/route_constants.dart';  
import '../../features/auth/signup/agreement/screens/agreement_screen.dart';  
import '../../features/home/home_screen.dart';  
import '../widgets/layout/app_shell.dart';  
  
class AppRouter {  
  static final GoRouter router = GoRouter(  
    initialLocation: RouteConstants.signin,  
    routes: [  
      // Without Shell route  
      GoRoute(  
        path: RouteConstants.signin,  
        builder: (context, state) => SigninScreen(),  
      ),  
      GoRoute(  
        path: RouteConstants.signup,  
        redirect: (context, state) =>  
        '/signup/agreement', // 자동으로 첫 단계로 리다이렉트  
      ),  
      GoRoute(  
        path: '/signup/agreement',  
        builder: (context, state) => AgreementScreen(  
          onNext: () => context.go('/signup/phone-verification'),  
        ),  
      ),  
  
      // With Shell route  
      ShellRoute(  
        builder: (context, state, child) => AppShell(child: child),  
        routes: [  
          // 메인 화면  
          GoRoute(  
            path: RouteConstants.home,  
            builder: (context, state) => HomeScreen(),  
          ),  
        ],  
      ),  
    ],  
  );  
}

앱 라우트 코드
import 'package:apt_user_flutter/features/auth/signin/signin_screen.dart';  
import 'package:go_router/go_router.dart';  
  
import '../../core/constants/route_constants.dart';  
import '../../features/auth/signup/agreement/screens/agreement_screen.dart';  
import '../../features/home/home_screen.dart';  
import '../../features/webview/webview_screen.dart';  
import '../../features/webview/webview_viewmodel.dart';  
import '../widgets/layout/app_shell.dart';  
  
class AppRouter {  
  static final GoRouter router = GoRouter(  
    initialLocation: RouteConstants.signin,  
    routes: [  
      // Without Shell route  
      GoRoute(  
        path: RouteConstants.webview,  
        name: '/webview',  
        builder: (context, state) {  
          final config = state.extra as WebViewConfig;  
          return WebViewScreen(config: config);  
        },  
      ),  
      GoRoute(  
        path: RouteConstants.signin,  
        builder: (context, state) => SigninScreen(),  
      ),  
      GoRoute(  
        path: RouteConstants.signup,  
        redirect: (context, state) =>  
        '/signup/agreement', // 자동으로 첫 단계로 리다이렉트  
      ),  
      GoRoute(  
        path: '/signup/agreement',  
        builder: (context, state) => AgreementScreen(  
          onNext: () => context.go('/signup/phone-verification'),  
        ),  
      ),  
      // With Shell route  
      ShellRoute(  
        builder: (context, state, child) => AppShell(child: child),  
        routes: [  
          // 메인 화면  
          GoRoute(  
            path: RouteConstants.home,  
            builder: (context, state) => HomeScreen(),  
          ),  
        ],  
      ),  
    ],  
  );  
}

앱 쉘 코드
// lib/core/widgets/layout/app_shell.dart  
import 'package:flutter/material.dart';  
import 'package:go_router/go_router.dart';  
import 'package:flutter_riverpod/flutter_riverpod.dart';  
import 'app_app_bar.dart';  
  
class AppShell extends ConsumerWidget {  
  final Widget child;  
  const AppShell({super.key, required this.child});  
  
  static const List<String> _routes = ['/playground', '/notice', '/home', '/my', '/all'];  
  
  @override  
  Widget build(BuildContext context, WidgetRef ref) {  
    final location = GoRouterState.of(context).uri.toString();  
    final int selectedIndex = _getSelectedIndex(location);  
  
    return Scaffold(  
      appBar: const AppAppBar(),  
      body: child,  
      bottomNavigationBar: BottomNavigationBar(  
        type: BottomNavigationBarType.fixed,  
        currentIndex: selectedIndex,  
        onTap: (index) => _onItemTapped(context, index),  
        items: const [  
          BottomNavigationBarItem(  
            icon: Icon(Icons.casino_outlined),  
            activeIcon: Icon(Icons.casino),  
            label: '놀이터',  
          ),  
          BottomNavigationBarItem(  
            icon: Icon(Icons.description_outlined),  
            activeIcon: Icon(Icons.description),  
            label: '관리소',  
          ),  
          BottomNavigationBarItem(  
            icon: Icon(Icons.home_outlined),  
            activeIcon: Icon(Icons.home),  
            label: '홈',  
          ),  
          BottomNavigationBarItem(  
            icon: Icon(Icons.chair_outlined),  
            activeIcon: Icon(Icons.chair),  
            label: '우리집',  
          ),  
          BottomNavigationBarItem(  
            icon: Icon(Icons.menu_outlined),  
            activeIcon: Icon(Icons.menu),  
            label: '전체보기',  
          ),  
        ],  
        selectedItemColor: Colors.teal[700],  
        unselectedItemColor: Colors.grey,  
        showUnselectedLabels: true,  
      ),  
    );  
  }  
  
  int _getSelectedIndex(String location) {  
    for (int i = 0; i < _routes.length; i++) {  
      if (location.startsWith(_routes[i])) return i;  
    }  
    return 0;  
  }  
  
  void _onItemTapped(BuildContext context, int index) {  
    context.go(_routes[index]);  
  }  
}


webview_flutter: ^4.13.0  
provider: ^6.1.5