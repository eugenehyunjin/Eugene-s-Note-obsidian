처리해야 할 일:
- [x] 서버 업데이트 관련 업무 체크c
- 243 서버 관련 문의 하기 - 김이사님 문의 완료, 확인 후 리눅스로 대체 
- 243 서버 리눅스로 작업 가능 확인 완료
- 243 서버 리눅스로 교체 가능 컨펌 완료, 일정 확인 필요
- [ ] 웹 홈페이지 레퍼런스 찾아보기, 기획 구성 초안 작성하기
- [x] 서버 별 제공 중인 서비스 파악해서 리스트로 정리 - 노션 정리 완료
- [x] 239 서버 용량 문제로 인해 처리 필요
- 기존 동서 joyapt 관련 코드 백업 후 삭제 처리
- 기존 쌓이고 있는 백엔드 로그 백업 처리 후 삭제 처리 - 처리 완료(공간 확보 완료)
- 기존 쌓이는 로그는 30일 이전의 코드는 삭제하도록 처리 완료(삭제 로직 추가)


앱 관련 업무:
- [x] 앱 버전 체크 로직 (스플래시 단에서 처리) - 플러터 플러그인 사용 가능 - 개발 완료 git release 적용 예정 수요일 오전
- [x] QA 리스트 만들기 - 노션에 작성 완료
- [x] 비케이위너 테스터에게 테스트 관련 공지 및 앱 다운로드 안내
- [x] 테스트 리스트 노션 사용법 안내 및 케이스 ID 작성

앱 추후 업데이트 작업:
- [ ] 준비중 페이지로 대체해주기 - 연지님 아이콘 관련해서 요청 완료 - 아이콘 구매 완료 - 이후 업데이트에 적용
- [ ] 프로필 닉네임 및 사진 변경 기능 - 이후 업데이트에 적용



동찬님: 소방점검 완료, 점검 어디에 배치를 할지, 오전 중에 끝날 예정
투표/설문 쪽을 진행할지? - 업무 집중 필요, 공지에서 링크로 넘어가게 처리하는 거
깃플로우 - 릴리즈에서 따기로, main에서 따지 않기로, core쪽 기능된거는 먼저 오려보기

혜림님: 백엔드 pss_no 받아서, 전자 고지서 스토리지에 올려서 테스트 해보기로, 관리비 완료, 전자고지서도 완료, Lpoint - 작업 시작, 기존앱 백엔드(리프레시 토큰, redux 파일에서 호출, 타이밍이 꼬이는)

연지님: 아파트 등록 안한 사람, 준회원 페이지, 아이콘 적절한 걸 찾기,
목,금 연차

상언님: 공지완료, 유저게시판(civil, board)

오전 미팅:
- 커뮤니티 관련 시설 미팅 진행
- 선라이즈 컨설팅(대구 소재지), APT 커뮤니티 관련 솔루션 제공하는 업체, 전체 시설에 대한 구축 진행하고, 웹으로 된 예약 관리 시스템 제공 중
- API를 통해 아파트 주민들의 동/호 정보를 넘겨서 SSO 처리가 가능한지 확인이 필요(개발자 미팅 완료, 가능하다는 답변 확인)
- 추후 계약 진행되는 경우, 전산 적인 부분 소통 필요함


L2S23 수도료감면
L2S24 전기료복지할인
L2S25 전기료대가족할인
L2S26 필수보장공제
L2S34 하계할인

Lpoint 관련:
239서버에서 aptree.kr 프론트 실행 중
lpoint.aptree.kr로 요청 가고, LPNT0001~5까지 요청 가능
김선기라는 친구가 만들었음(행복한 아파트 앱)
블록체인 발주 받았던 와이프가 엘포인트 개발을 진행
6억 15억 개발 투자 받음(김종훈 이사 투입)

구글 확장프로그램 모멘텀


<VirtualHost *:80>
        ServerName aptree.co.kr
        ServerAlias www.aptree.co.kr
        ServerAlias abang.co.kr
        ServerAlias www.abang.co.kr
        ServerAlias lpoint.aptree.kr

        #ServerAdmin infra@qdomain.net
        DocumentRoot /home/aptree/frontend

        #LogLevel info ssl:warn

        ErrorLog ${APACHE_LOG_DIR}/aptree.co.kr-error.log
        #CustomLog ${APACHE_LOG_DIR}/access.log

        ProxyRequests Off
        #ProxyPass /swagger http://localhost:8080/swagger
        ProxyPass /rest/data/lpoint http://211.239.22.33:8080/rest/data/lpoint
        ProxyPass /rest http://localhost:8080/rest
        ProxyPass /api http://localhost:8080/api
        ProxyPass /oz80 http://211.239.22.33:38080/oz80
</VirtualHost>
# vim: syntax=apache ts=4 sw=4 sts=4 sr noet

<VirtualHost *:443>
        ServerName aptree.co.kr
        ServerAlias www.aptree.co.kr app.aptree.co.kr

        ServerAdmin infra@qdomain.net
        DocumentRoot /home/aptree/frontend

        ErrorLog ${APACHE_LOG_DIR}/aptree.co.kr-error.log
        #CustomLog ${APACHE_LOG_DIR}/access.log

        SSLEngine on
        #SSLCertificateFile /etc/letsencrypt/live/aptree.co.kr/fullchain.pem
        #SSLCertificateKeyFile /etc/letsencrypt/live/aptree.co.kr/privkey.pem

        SSLProtocol all -SSLv2 -SSLv3
        SSLCipherSuite HIGH:MEDIUM:!SSLv2:!PSK:!SRP:!ADH:!AECDH

        #SSLCertificateFile /etc/apache2/sslkey/cert.pem
        #SSLCertificateKeyFile /etc/apache2/sslkey/key.pem
        #SSLCertificateChainFile /etc/apache2/sslkey/DigiCertCA.pem
        SSLCertificateFile /etc/apache2/sslkey/WILD.aptree.co.kr/Apache/cert.pem
        SSLCertificateKeyFile /etc/apache2/sslkey/WILD.aptree.co.kr/Apache/key.pem
        SSLCertificateChainFile /etc/apache2/sslkey/WILD.aptree.co.kr/Apache/DigiCertCA.pem

        ProxyRequests Off
        ProxyPass /swagger-ui.html http://localhost:8080/swagger-ui.html
        ProxyPassReverse /swagger-ui.html http://localhost:8080/swagger-ui.html

        proxyPass /swagger http://localhost:8080/swagger
        ProxyPass /rest/data/lpoint http://211.239.22.33:8080/rest/data/lpoint

        ProxyPass /rest http://localhost:8080/rest
        ProxyPassReverse /rest http://localhost:8080/rest

        ProxyPass /api http://localhost:8080/api
        ProxyPassReverse /api http://localhost:8080/api

        ProxyPass /oz80 http://211.239.22.33:38080/oz80
</VirtualHost>

<VirtualHost *:443>
        ServerName abang.co.kr
        ServerAlias www.abang.co.kr

        ServerAdmin infra@qdomain.net
        DocumentRoot /home/aptree/frontend

        ErrorLog ${APACHE_LOG_DIR}/aptree.co.kr-error.log

        SSLEngine on
        SSLProtocol all -SSLv2 -SSLv3
        SSLCipherSuite HIGH:MEDIUM:!SSLv2:!PSK:!SRP:!ADH:!AECDH

        SSLCertificateFile /etc/letsencrypt/live/abang.co.kr/cert.pem
        SSLCertificateChainFile /etc/letsencrypt/live/abang.co.kr/fullchain.pem
        SSLCertificateKeyFile /etc/letsencrypt/live/abang.co.kr/privkey.pem

        ProxyRequests Off
        #ProxyPass /swagger http://localhost:8080/swagger
        ProxyPass /rest/data/lpoint http://211.239.22.33:8080/rest/data/lpoint
        ProxyPass /rest http://localhost:8080/rest
        ProxyPass /api http://localhost:8080/api
        ProxyPass /oz80 http://211.239.22.33:38080/oz80
</VirtualHost>

<VirtualHost *:443>
        ServerName lpoint.aptree.kr

        ServerAdmin infra@qdomain.net
        DocumentRoot /home/aptree/frontend

        ErrorLog ${APACHE_LOG_DIR}/aptree.co.kr-error.log

        SSLEngine on
        SSLProtocol all -SSLv2 -SSLv3
        SSLCipherSuite HIGH:MEDIUM:!SSLv2:!PSK:!SRP:!ADH:!AECDH

        SSLCertificateFile /etc/letsencrypt/live/lpoint.aptree.kr/cert.pem
        SSLCertificateChainFile /etc/letsencrypt/live/lpoint.aptree.kr/fullchain.pem
        SSLCertificateKeyFile /etc/letsencrypt/live/lpoint.aptree.kr/privkey.pem

        ProxyRequests Off
        #ProxyPass /swagger http://localhost:8080/swagger
        ProxyPass /rest/data/lpoint http://211.239.22.33:8080/rest/data/lpoint
        ProxyPass /rest http://localhost:8080/rest
        ProxyPass /api http://localhost:8080/api
        ProxyPass /oz80 http://211.239.22.33:38080/oz80
</VirtualHost>


<Directory /home/aptree/frontend/>
        Options Indexes FollowSymLinks
        AllowOverride All
        Require all granted
        RewriteEngine On
        EnableSendfile Off
        RewriteBase /
        RewriteRule ^index\.html$ - [L]
        RewriteCond %{REQUEST_FILENAME} !-f
        RewriteCond %{REQUEST_FILENAME} !-d
        RewriteCond %{REQUEST_FILENAME} !-l
        RewriteRule . /index.html [L]
</Directory>

239서버 관련:
1. crontab 스케쥴러 코드
명령어: crontab -l
0 1 * * * su - aptree -c "/home/aptree/backend/daily_restart.sh"
0 2 * * * su - aptree -c "/home/aptree/AlarmDaemon/daily_restart.sh"

3. frontend 파일 배포 중
프로젝트에서 npm run build를 통해 빌드하고, 빌드된 파일 업로드하여 배포중
`index.html`, `asset-manifest.json`, `static/`

3. 리눅스에서 시스템 타이머를 사용해서 ssl letsencrypt renew 중
명령어:  systemctl list-timers | grep certbot
Tue 2025-08-26 14:37:00 KST 4h 28min left Tue 2025-08-26 09:20:01 KST 48min ago     snap.certbot.renew.timer     snap.certbot.renew.service
Tue 2025-08-26 21:10:22 KST 11h left      Tue 2025-08-26 01:27:33 KST 8h ago        certbot.timer                certbot.service
- 설정 파일 경로: /etc/letsencrypt/renewal/lpoint.aptree.kr.conf


아파트리 개발 이력 및 순서:
1. 동서에서 유피체인을 통해서 앱, 웹 모두 개발(스프링부트, 리액트, 리눅스 기반)
- 239 서버에 Backend(Spring boot - 앱(입주민,관리소), 웹(관리자, Lpoint)), Frontend(React - 관리자 페이지 등) 개발하여 배포 실행 중
- Apache2 를 통해 프론트 배포 중(app.aptree.co.kr, lpoint.aptree.kr 도메인 연결 됨) - 무료 letsencrypt를 System Timer를 통해서(스케쥴러) certbot renew 매일 실행 하여 갱신 중
- Backend jar 파일 실행 중(app.aptree.co.kr 도메인 연결 됨) - 유료 SSL 인증서 적용 중, 최근까지 부산웹에서 관리했을 것으로 추측 - 매년 유료 인증서(aptree.co.kr)로 갱신 필요
- Crontab으로 아래의 파일 매일 실행 중
	0 1 * * * su - aptree -c "/home/aptree/backend/daily_restart.sh"
	0 2 * * * su - aptree -c "/home/aptree/AlarmDaemon/daily_restart.sh"

2. 권대표님이 본격적으로 시스템 재정비
- 블록체인 사업과 연동하여 재정비 진행(RND 선정 되어 급하게 개발 진행)
- 기존 239 서버에서 모두 관리 중인 부분에서, 241~245 서버 재 구축(윈도우 서버)
- 부산웹과 웹 표준화 작업 진행 - 기존 리액트 소스 코드를 PHP로 전환하고, DB 통합
- 부산웹은 최초 239 리눅스에 개발 진행, 동서에서 리눅스 개발자가 부재하여 윈도우로 전부 교체 진행
	- 기존 리액트 앱은 사용을 멈추고, 부산웹에서 개편한 php 웹으로 전환 완료
- 추가 된 사내 개발자들 김종훈 이사의 리드로 윈도우 서버에 기존 소스 코드들 재개발 진행
	- 입주민/관리소 앱 기존 리액트 네이티브에서 소스 코드 수정 진행(지금의 코드 ㅜㅠ)


기술 스택의 변화:
1. 앱: React-native, 웹: React, 백엔드: Spring boot, DB: mysql, 서버: 리눅스
2. 앱: React-native, 웹: PHP(백엔드 직접 연동), 백엔드: Spring boot(앱만 사용), DB: mariaDB, 서버: 리눅스에서 윈도우 서버로 전환
3. 앱: Flutter, 웹: PHP, 백엔드: Spring boot(입주민 renew, 관리소 기존 Backend 유지), DB: mariaDB, 서버: 리눅스 1대, 윈도우 5대

 현재: 앱은 전부 Flutter로 전환, 백엔드는 최신 Spring Boot로 변경, 입주민과 관리소 처리하는 백엔드 분리, DB: mariaDB 유지, 서버: 리눅스 2대, 윈도우 4대로 전환(리눅스: real/test 각 1대, 윈도우: test 1대, real)

239서버 관련:
- 현재 남아있는 joyapt 관련된 코드는 모두 삭제 가능(08/27박창은 이사님 컨펌 완료)\
- 백업 완료 후에, 서버에서 삭제 진행
- 그 외 백엔드, AlarmDaemon 로그 파일 전부 삭제
- 용량 확보 작업이 필요함

mount -t cifs -o username='upload',password='upl12@$!',noperm,rw //203.235.19.241/apt_fileUpload /home/aptree/apt_fileUpload_new

sudo mkdir -p /home/aptree/backup_mount

sudo mount -t cifs -o username='upload',password='upl12@$!',noperm,rw //203.235.19.241/backup /home/aptree/backup_mount


find /home/aptree/backend/backup/ -type f -name "nohup.out_*" -mtime +30 -delete


cd /home/aptree/backend

DATE=$(date +"%Y%m%d")

kill -9 `ps -ef|grep apt_project_jpa-0.0.1-SNAPSHOT.jar|awk '{print $2}'`
mv nohup.out ./backup/nohup.out_$DATE
find ./backup/ -type f -name "nohup.out_*" -mtime +30 -delete
nohup java -jar apt_project_jpa-0.0.1-SNAPSHOT.jar > /home/aptree/backend/nohup.out 2>&1


NAME="Ubuntu"
VERSION="20.04.4 LTS (Focal Fossa)"
ID=ubuntu
ID_LIKE=debian
PRETTY_NAME="Ubuntu 20.04.4 LTS"
VERSION_ID="20.04"
HOME_URL="https://www.ubuntu.com/"
SUPPORT_URL="https://help.ubuntu.com/"
BUG_REPORT_URL="https://bugs.launchpad.net/ubuntu/"
PRIVACY_POLICY_URL="https://www.ubuntu.com/legal/terms-and-policies/privacy-policy"
VERSION_CODENAME=focal
UBUNTU_CODENAME=focal


ps -ef | grep java
3404528

금요일 회의:
1. 오류 처리 방식:
	- 크리티컬한 문제가 아니라면,공통적인거, 지금 작업하지 않아도 되는거, 우선순위를 정해서 따로 처리하는 방법?
	- 플러터에서 UI랑 로직 분리가 애매해지는 부분이 있음
	- 위젯이 로직을 포함하고 있는 경우? 
	- 학습에 대한 결정을 빨리 내리고 서로가 필요한 부분에 대해서 의견을 계속 공유 하는 방향
	- 인프런 강의 등록하고 
	- 딥하게 다른 사람의 소스 코드 체크해서 어떤 방식으로 코드가 짜져있는지 체크 해보기
	- 테스트 동시 진행
2. 혜림님:
	- lpoint는 서버 33을 거쳐야 하는 가?(박창은 이사님한테 한 번 더 체크해보기, 전체 로직이 동서에서 처리하고 있는 이유)
	- 243 서버 리눅스 교체 하기
	- 관리사무소 앱 안되는 부분 - 센터에서 관리사무소 앱 수정한 내용이 잘 불러와지는지 확인 필요
	- 오후 5시 이후로 기존 서버 테스트 필요
3. 동찬님: 누군가 작업한 내용을 어떻게 공유할 것인가?
	 - 깃 코어 우선 커밋 하는 방법 공유
