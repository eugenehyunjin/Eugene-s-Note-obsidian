처리할 일:
- [ ] 웹 홈페이지 레퍼런스 찾아보기, 기획 구성 초안 작성하기
- [x] 8월 근무표 과장님에게 전달하기
- [x] 수요일, 금요일 오후 회의실 잡기
- [x] 8월 전체 홈페이지 데이터 업데이트 하기
- [x] 4일 점심에 센터 분들 노션 QA 리스트 작성 방법 줌으로 전달 하기 -- 현장에서 바로 전달 완료

앱 관련 업무:
- [x] 플러터 관련 강의 찾아서 공유 - 구매 완료
- [ ] 코드 리뷰 및 개선
- [x] 테스트 리스트 작성
- [x] 차량 8380 코드 오류 개선 패치 - 완료 후, 넥스파 시스템 담당자에게 last_fached_idx 값 전달 필수임을 다시 안내, 처리 완료 
- [x] 기존 앱에서 전자투표 차트 그리드 깨진는 부분 오류 수정 - 수정 완료 후, 깃에 배포 완료

앱 추후 업데이트 작업:
- [ ] 준비중 페이지로 대체해주기 - 연지님 아이콘 관련해서 요청 완료 - 아이콘 구매 완료 - 이후 업데이트에 적용
- [ ] 프로필 닉네임 및 사진 변경 기능 - 이후 업데이트에 적용


앱 회원가입 오류:

이미 가입한 세대주가 있습니다. 
C:\Users\BKW_RnD_2\.jdks\ms-21.0.7


```
<?php
// 여기서 로그인 사용자 정보를 세션에서 저장.
$nickname = '이현진';      // 예: $_SESSION['nickname']
$userId   = 'eugene12';   // 예: $_SESSION['user_id']

// 위의 값을 post로 전달 받을 url
$targetUrl = '{SSO 처리를 할 mall의 페이지 경로,예: https://aptree.co.kr/home/mall/sso/loginSsoGate.php}';

// 로그인 처리 후, 리다이렉트 할 페이지 경로(특정 상품으로 바로 연결을 위한 파라미터)
$nextPage = 'https://aptree.co.kr/mall/search/main?keyword=검색어';

$params = [
    'userName'  => $nickname,
    'userId'  => $userId,
    'nextPage'     => $nextPage,
    //필요하다면 아파트리 DB의 Users의 인덱스 값을 전달 가능
];
?>

위의 방식으로 SSO 처리를 할 mall의 페이지로 post 요청하면, 해당 페이지에서 245DB의 user를 바로 검색해서 해당 user의 정보를 mall DB의 user에 등록(회원가입 처리)해 주시고, 추가로 로그인 처리 한 다음 메인 화면으로 리다이렉트 혹은 전달 받은 nextPage값이 있다면 해당 페이지로 리다이렉트 처리.

현재는 위의 방식으로 이제너두몰에 SSO 연동이 되어있습니다. 
아래는 기존 php 홈페이지에서 스크립트 방식으로 복지몰에 연결하는 코드입니다. 참고해주실 수 있습니다. 
  
        $(function() {
            $(".benecafe_link").click(function() {
                //연결될 url 정보
                const lcValue = $(this).data('lc');
                $("#cafeform").remove();
                <?php if (($_SESSION['my_idx'] ?? '') == '') { ?>
                    alert("로그인 후 이용 가능합니다.");
                <?php } else {
                    // memID 값 설정 로직
                    $memID = $_SESSION['my_id'] . '_' . $_SESSION['my_dancode'];
                    if ($_SESSION['my_group_type'] == 'ADMIN' && $_SESSION['my_group_name'] == 'POST_OFFICES') {
                        $memID .= '_admin';
                    }
                ?>
                    let formHtml = `
                    <form method="post" action="https://cert.benecafe.co.kr/sso/loginSsoGate" id="cafeform" name="cafeform" target="_blank">

                    <input type="hidden" id="cmpyNo" name="cmpyNo" value="QB1" />

                    <input type="hidden" id="mbrNm" name="mbrNm" value="<?= $_SESSION['my_name'] ?>" />
                    <input type="hidden" id="memID" name="memID" value="<?= $memID ?>" />
                `;
       // lc 데이터가 있는 경우만 추가
                    if (lcValue) {
                        formHtml += `<input type="hidden" id="lc" name="lc" value="${lcValue}" />`;
                    }
                    formHtml += `</form>`;
                    // 폼 추가 및 전송
                    $("body").append(formHtml);
                    $("#cafeform").submit();
                <?php } ?>
                return false;
            });
        });
```


차량 방문 8380 오류 정리:
1. last_fetched_idx를 입력해도 null로 체크 됨
2. 마지막 인덱스 반환이 안됨 - 아래의 쿼리 order by 정렬 추가 로직으로 해결 가능
```
<select id="visitVehiclesInitialIdx" resultType="com.aptree.module.data.Result">
    SELECT
        VV.APARTMENT_IDX,
        VV.IDX AS VISITING_VEHICLES_IDX,
        VV.CAR_NUMBER,
        DATE_FORMAT(VV.SCHEDULED_DATE,'%Y-%m-%dT%H:%i:%s') AS SCHEDULED_DATE,
        DATE_FORMAT(VV.OUT_DATE, '%Y-%m-%dT%H:%i:%s') AS OUT_DATE,
        D.DONG,
        D.HO
    FROM
        VISITING_VEHICLES VV
            JOIN
        DONGHOS D ON VV.DONGHO_IDX  = D.IDX AND D.APARTMENT_IDX = VV.APARTMENT_IDX
    WHERE
        VV.APARTMENT_IDX =  #{apartment_idx}
      AND VV.USE_FLAG = 'Y'
      AND VV.OUT_DATE >= NOW()
      AND NOT EXISTS (
        SELECT 1
        FROM VEHICLE_HISTORIES VH
        WHERE
            VH.APARTMENT_IDX = VV.APARTMENT_IDX
          AND VH.CAR_NUMBER = VV.CAR_NUMBER
          AND VH.IN_DATE IS NOT NULL
    )
    ORDER BY VV.IDX DESC
</select>
```
3. findInitialIdx가 2번 실행되는 로직으로 인한 오류 - 처리 필요


postman 차량 관제 테스트 url
http://203.235.19.242:8380/visit-vehicles/5605?last_fetched_idx=199469

사장님 회의:


5605 단지 차량 로그
201222 |20990         |93고0902    |205  |1202 |2025-09-04T21:30:00 |2025-09-06T15:30:00 

블록체인 성과지표 요구

다음주 업무:
코드정리
- 전반적인 느낌, 전체 적인 그림이 필요할 것 같음
- 목표가 무엇인지, 해야될 일들
- 우선순위가 처리 하려면 시간을 가져가야 될 것 같음
- 업무 담당을 배정을 하고, qa는 언제 할 건지, 외/내부 테스트를 언제 할 건지 결정
- 내부 / 외부 qa 맡기고 2차 적으로 전체 flow

2번째:
- 새로 만들자 - 이유? 시작을 할 때 느낌으로 시작? 아무것도 없고 기획을 하는 것도 중요
- 혼돈 속에서 하고 있다고 생각됨, 나열이 되어있으니까 끝나면 어떤 걸 해야 겠다라는 스케쥴이 없다
- 뒤에 얼마나 남았는지 모르니까 그런 것들을 조율을 해야 한다.
- 페이지에 들어가서 유즈케이스나, 기능 명세가 필요하다.
- 일관성을 가진 다음에 갈아엎자 - 고도화 하고 개선을 하자!
- 현상태를 정리를 먼저 하자 - 어디까지 만들어야 하고 어디까지 만들어놓았고 현상태에 대한 정리
- 앱 개편에 대한 업무말고도 다른 업무에 대한 우선순위가 필요
혜림씨 의견:
- 고민이 없이 진행을 했다. - 유지보수를 편하게 하고자 고민을 하면서 코드를 각자가 쳤다고는 생각안함
- 플러터 시작을 했을 때도 강의를 들으려면, 그 때 들었어야 함, 한 사람이 기준을 잡고 가야 된다고 생각함
- 소리가 안나게 하면서 하는게 아니다.
- 금요일 마다 회의를 할 때도, 기능 구현했고 발전되는 컨펌해줄 수 있는 시니어 개발자가 없다.
- 주간 마다 개발을 했던 것도 한사람의 기준을 잡는 것에 포함이 된다고 생각함
- 누군가가 코어 만드는 것처럼 이런것들을 만들어 주고, 어떻게 쓰면 됩니다 하고 안내하는 것이었음
동찬님 의견:
- 금요일 회의에서 말한 내용들은 일종의 코드 리뷰였다고 생각
- 한사람이 잡고 가자는 한명이 모든 코드를 보고 자기가 생각한 구조대로 일관성 있게 만들어두는게 필요하다는 건지?
- 상언님이 말한 거랑 똑같다, 누군가 뭐든 간에 그렇게 진행하는 것도 동의 한다. 한 명이 해결사가 되는 것도 문제 없음

플러터 구조 - 

회의 의견:
1. 데드라인 없는 거 - 불안감을 느끼고 있음
2. 정확한 목표 공유가 없다 - 운영진과의 일정 공유가 안되고 있다.
3. 맺고 끊음이 없다, 프로토타입 - 디벨롭하고 있음, 컴포넌트가 적용이 안되고 있음
4. 첫단추부터 딱 정해 이렇게 갈거야 흐지부지, 다 각자 만들고 있음
5. 물리적인 시간이 존중이 안됨 - 다 엉망임, 시간이 없음, 해본 경험이 없어서, 그 5분 조차도 주어지지 않음
6. 11월까지 정확하게 만들어져야 하는 게 명확하게 나온건지?
7. 일정이 미지수인 부분이 반복되는 느낌
